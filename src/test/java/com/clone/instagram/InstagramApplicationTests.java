package com.clone.instagram;


import com.clone.instagram.dao.UserDao;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

//@RunWith(SpringRunner.class) => Junit4 사용시 추가, Junit5 사용시 생략. @SpringBootTest( properties = { "propertyTest.value=propertyTest", "testValue=test" } )
@SpringBootTest
@RunWith(SpringRunner.class)
public class InstagramApplicationTests {


}
